export const MATCH_STATUS = {
    PENDING: 'pending',
    ACCEPTED: 'accepted',
    REJECTED: 'rejected',
    SHORTLISTED: 'shortlisted',
};

export const ALL_MATCH_STATUSES = Object.values(MATCH_STATUS);
